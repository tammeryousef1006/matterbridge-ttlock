/**
 * @license
 * Copyright 2022-2025 Matter.js Authors
 * SPDX-License-Identifier: Apache-2.0
 */
import { ClusterModel } from "../../models/index.js";
export type FeatureFlags = string[];
export type FeatureBitmap = {
    [name: string]: boolean;
};
export type FeatureNames = {
    [key: string]: string;
};
export declare function FeatureBitmap(bitmap?: FeatureBitmap | FeatureFlags): FeatureBitmap;
/**
 * Map the names in a feature bitmap to different names.  Used to convert from
 * a feature code bitmap to a feature name bitmap.
 *
 * E.g. { F: true } -> { foo: true }
 *
 * If a name isn't present leaves the feature code intact.
 */
export declare function translateBitmap(bitmap: FeatureBitmap, cluster: ClusterModel): {
    [k: string]: boolean;
};
//# sourceMappingURL=FeatureBitmap.d.ts.map