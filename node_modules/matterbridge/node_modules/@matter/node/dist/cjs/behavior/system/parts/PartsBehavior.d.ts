/**
 * @license
 * Copyright 2022-2025 Matter.js Authors
 * SPDX-License-Identifier: Apache-2.0
 */
import { Agent } from "#endpoint/Agent.js";
import { Endpoint } from "#endpoint/Endpoint.js";
import { MutableSet } from "#general";
import { Behavior } from "../../Behavior.js";
/**
 * Provides access to child endpoints.
 */
export declare class PartsBehavior extends Behavior implements MutableSet<Endpoint, Endpoint | Agent> {
    static readonly id = "parts";
    add(child: Endpoint.Definition | Agent): void;
    delete(child: Endpoint | Agent): boolean;
    clear(): void;
    has(child: Endpoint | Agent): boolean;
    indexOf(child: Endpoint | Agent): number;
    [Symbol.iterator](): Generator<Agent.Instance<import("../../../index.js").EndpointType.Empty>, void, unknown>;
}
//# sourceMappingURL=PartsBehavior.d.ts.map