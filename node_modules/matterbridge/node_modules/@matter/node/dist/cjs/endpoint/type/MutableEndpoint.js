"use strict";
var __defProp = Object.defineProperty;
var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
var __getOwnPropNames = Object.getOwnPropertyNames;
var __hasOwnProp = Object.prototype.hasOwnProperty;
var __export = (target, all) => {
  for (var name in all)
    __defProp(target, name, { get: all[name], enumerable: true });
};
var __copyProps = (to, from, except, desc) => {
  if (from && typeof from === "object" || typeof from === "function") {
    for (let key of __getOwnPropNames(from))
      if (!__hasOwnProp.call(to, key) && key !== except)
        __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
  }
  return to;
};
var __toCommonJS = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);
var MutableEndpoint_exports = {};
__export(MutableEndpoint_exports, {
  MutableEndpoint: () => MutableEndpoint
});
module.exports = __toCommonJS(MutableEndpoint_exports);
var import_SupportedBehaviors = require("../properties/SupportedBehaviors.js");
var import_EndpointType = require("./EndpointType.js");
/**
 * @license
 * Copyright 2022-2025 Matter.js Authors
 * SPDX-License-Identifier: Apache-2.0
 */
function MutableEndpoint(options) {
  const type = (0, import_EndpointType.EndpointType)(options);
  let defaults;
  return {
    ...type,
    get defaults() {
      if (!defaults) {
        defaults = {};
        for (const name in type.behaviors) {
          defaults[name] = type.behaviors[name].defaults;
        }
      }
      return defaults;
    },
    set(defaults2) {
      const newBehaviors = Array();
      for (const name in this.behaviors) {
        const updates = defaults2[name];
        const behavior = this.behaviors[name];
        if (updates) {
          newBehaviors.push(behavior.set(updates));
        }
      }
      return this.with(...newBehaviors);
    },
    with(...behaviors) {
      return MutableEndpoint({
        ...options,
        behaviors: import_SupportedBehaviors.SupportedBehaviors.extend(this.behaviors, behaviors)
      });
    },
    withBehaviors(...behaviors) {
      return this.with(...behaviors);
    }
  };
}
//# sourceMappingURL=MutableEndpoint.js.map
