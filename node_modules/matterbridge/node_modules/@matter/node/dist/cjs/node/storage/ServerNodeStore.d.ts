/**
 * @license
 * Copyright 2022-2025 Matter.js Authors
 * SPDX-License-Identifier: Apache-2.0
 */
import { Destructable, Environment } from "#general";
import { ClientStoreService } from "./ClientStoreService.js";
import { NodeStore } from "./NodeStore.js";
/**
 * The "server" node store is a {@link NodeStore} with storage for components shared by the server node and client
 * nodes.
 */
export declare class ServerNodeStore extends NodeStore implements Destructable {
    #private;
    constructor(environment: Environment, nodeId: string);
    static create(environment: Environment, nodeId: string): Promise<ServerNodeStore>;
    close(): Promise<void>;
    get clientStores(): ClientStoreService;
    initializeStorage(): Promise<void>;
}
//# sourceMappingURL=ServerNodeStore.d.ts.map